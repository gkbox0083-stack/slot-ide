# slot-ide V2 產品需求規格書（PRD）

> **版本**: 2.0  
> **日期**: 2026-01-07  
> **狀態**: 需求確認完成，待開發  

---

## 📋 目錄

1. [產品概述](#一產品概述)
2. [模組 A：UI/UX 設計模組](#二模組-aui-ux-設計模組)
3. [模組 B：遊戲機制模組](#三模組-b遊戲機制模組)
4. [模組 C：數據與統計模組](#四模組-c數據與統計模組)
5. [模組 D：用戶功能模組](#五模組-d用戶功能模組)
6. [模組 E：基礎設施模組](#六模組-e基礎設施模組)
7. [型別定義擴展](#七型別定義擴展)
8. [開發階段規劃](#八開發階段規劃)
9. [驗收清單](#九驗收清單)

---

## 一、產品概述

### 1.1 產品定位

| 項目 | 說明 |
|------|------|
| 產品名稱 | slot-ide V2 |
| 產品類型 | 對外產品（公開使用） |
| 目標用戶 | Slot 遊戲設計師、數學驗證人員、遊戲企劃 |
| 核心價值 | 可視化設計 Slot 遊戲數學模型，即時預覽與驗證 |

### 1.2 核心功能清單

| 功能 | 優先級 | 說明 |
|------|--------|------|
| 三欄式 Tab 面板 UI | P0 | 全新佈局架構 |
| Wild 符號 | P0 | 替代一般符號 |
| Scatter 符號 + Free Spin | P0 | 觸發免費旋轉 |
| NG/FG 獨立 Outcome 配置 | P0 | 業界標準 RTP 計算 |
| 5x3 / 5x4 盤面切換 | P0 | 動態盤面支援 |
| Pay Lines 視覺化編輯 | P0 | 最多 50 條線 |
| 用戶自定義 Symbol | P0 | 新增/刪除符號 |
| Firebase 用戶系統 | P1 | Google OAuth 登入 |
| 模板管理 | P1 | 雲端儲存與讀取 |
| Dashboard | P1 | 用戶模板列表 |
| Simulation 堆疊/比較 | P1 | 累加模擬結果 |
| 數值驗證工具 | P2 | 賠率分佈圖、矛盾警示 |
| 深色模式 | P3 | 可選功能 |

### 1.3 技術架構約束

繼承 MVP 的核心架構原則：

- ✅ 單一 Vite + React App
- ✅ SpinPacket 是唯一資料合約
- ✅ Math Engine 是唯一真相來源
- ✅ Runtime 只負責渲染，不生成邏輯
- ❌ 禁止 iframe / postMessage
- ❌ 禁止第二套 RNG

### 1.4 雙層機率模型（新增架構）

```
┌─────────────────────────────────────────────────────────────┐
│  雙層機率模型                                                │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  【數學層】→ 影響 RTP                                        │
│  ├─ Outcome 權重：決定抽中哪個倍率區間                        │
│  └─ Pool 生成：基於 Outcome 倍率區間生成盤面                  │
│                                                             │
│  【視覺層】→ 不影響 RTP                                      │
│  └─ Symbol appearanceWeight：只影響滾動動畫中的符號分布       │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

**關鍵原則**：
- Pool Builder 只根據 Outcome 倍率區間和 Symbol Payouts 生成盤面
- `appearanceWeight` 只用於 Runtime 滾動動畫，不影響最終結果
- RTP 計算只看數學層

---

## 二、模組 A：UI/UX 設計模組

### 2.1 三欄式佈局架構

```
┌──────────────────────────────────────────────────────────────────────┐
│  🎰 slot-ide              [模板名稱▼]        [👤 用戶] [⚙️] [?]      │
├──────────────────┬────────────────────────────┬──────────────────────┤
│                  │                            │                      │
│  CONTROL PANEL   │      SLOT MACHINE          │    GAME CONTROL      │
│     (25%)        │         (50%)              │       (25%)          │
│                  │                            │                      │
│ ┌──────────────┐ │    ┌──────────────────┐    │  ┌────────────────┐  │
│ │[數值][視覺][Pool]│ │    │                  │    │  │[SPIN][SIM][MAX]│  │
│ └──────────────┘ │    │   ┌───┬───┬───┐   │    │  └────────────────┘  │
│                  │    │   │   │   │   │   │    │                      │
│  Tab 內容區域     │    │   ├───┼───┼───┤   │    │ ┌──────────────────┐ │
│  (可滾動)        │    │   │   │   │   │   │    │ │[Betting][Sim][His]│ │
│                  │    │   ├───┼───┼───┤   │    │ │[Free Spin]       │ │
│                  │    │   │   │   │   │   │    │ └──────────────────┘ │
│                  │    │   └───┴───┴───┘   │    │                      │
│                  │    │                  │    │  Tab 內容區域         │
│                  │    │  Win: $XXX       │    │  (可滾動)            │
│                  │    │  [中獎線資訊]     │    │                      │
│                  │    └──────────────────┘    │                      │
│                  │                            │                      │
├──────────────────┴────────────────────────────┴──────────────────────┤
│  📊 STATISTICS & CHARTS (可收合)                              [▲]    │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐                     │
│  │ Winnings    │ │ Bet History │ │ Symbol Dist │                     │
│  │ 柱狀圖      │ │ 折線圖      │ │ 圓餅圖      │                     │
│  └─────────────┘ └─────────────┘ └─────────────┘                     │
└──────────────────────────────────────────────────────────────────────┘
```

### 2.2 左側 Control Panel (25%)

#### 2.2.1 頂部 Tab 切換器

```typescript
type ControlPanelTab = 'numeric' | 'visual' | 'pool';
```

| Tab | 名稱 | 圖示 | 說明 |
|-----|------|------|------|
| `numeric` | 數值設定 | 🔢 | Outcomes、Symbols、Pay Lines |
| `visual` | 視覺設定 | 🎨 | 動畫、佈局、素材 |
| `pool` | Pool | 🎲 | 盤面模式、Build、狀態 |

#### 2.2.2 Tab: 數值設定

**Outcomes 面板**

```
┌─────────────────────────────────┐
│ Outcomes                        │
│ ┌─────────────────────────────┐ │
│ │ [Base Game ▼] [Free Game]   │ │  ← 切換器
│ └─────────────────────────────┘ │
│                                 │
│ ┌─────────────────────────────┐ │
│ │ 未中獎                       │ │
│ │ 倍率: 0x - 0x  權重: 600    │ │
│ │ [編輯] [刪除]               │ │
│ └─────────────────────────────┘ │
│ ┌─────────────────────────────┐ │
│ │ 小獎                         │ │
│ │ 倍率: 1x - 10x  權重: 300   │ │
│ │ [編輯] [刪除]               │ │
│ └─────────────────────────────┘ │
│ ...                             │
│ [+ 新增 Outcome]                │
└─────────────────────────────────┘
```

**Symbols 面板**

```
┌─────────────────────────────────┐
│ Symbols                         │
│                                 │
│ ┌─────────────────────────────┐ │
│ │ [圖] H1 皇冠                 │ │
│ │ 類別: [high ▼]              │ │
│ │ 3連: 50  4連: 100  5連: 200 │ │
│ │ NG權重: 10   FG權重: 10     │ │
│ │ [編輯] [刪除]               │ │
│ └─────────────────────────────┘ │
│                                 │
│ ┌─────────────────────────────┐ │
│ │ [圖] WILD 百搭              │ │
│ │ 類別: [wild ▼]              │ │  ← 選擇 wild 後顯示額外配置
│ │ ┌───────────────────────┐   │ │
│ │ │ ⚙️ Wild 設定           │   │ │
│ │ │ ☑ 替代一般符號         │   │ │
│ │ │ ☐ 替代特殊符號         │   │ │  ← 預設關閉
│ │ └───────────────────────┘   │ │
│ │ NG權重: 5    FG權重: 15     │ │
│ │ [編輯] [刪除]               │ │
│ └─────────────────────────────┘ │
│                                 │
│ ┌─────────────────────────────┐ │
│ │ [圖] SCATTER 分散           │ │
│ │ 類別: [scatter ▼]           │ │  ← 選擇 scatter 後顯示額外配置
│ │ ┌───────────────────────┐   │ │
│ │ │ ⚙️ Scatter 設定        │   │ │
│ │ │ 觸發數量: [3 ▼]        │   │ │
│ │ │ Free Spin 次數: [10]   │   │ │
│ │ │ ☑ 啟用 Retrigger       │   │ │
│ │ │ ☑ 啟用 Multiplier      │   │ │
│ │ │   倍率: [2x ▼]         │   │ │
│ │ └───────────────────────┘   │ │
│ │ NG權重: 3    FG權重: 5      │ │
│ │ [編輯] [刪除]               │ │
│ └─────────────────────────────┘ │
│                                 │
│ [+ 新增 Symbol]                 │
└─────────────────────────────────┘
```

**Pay Lines 面板**

```
┌─────────────────────────────────┐
│ Pay Lines                       │
│                                 │
│ 線數: [20 ▼] (最多 50 條)       │
│                                 │
│ ┌─────────────────────────────┐ │
│ │    視覺化線路編輯器           │ │
│ │                             │ │
│ │   [0,0] [1,0] [2,0] [3,0] [4,0] │
│ │   [0,1] [1,1] [2,1] [3,1] [4,1] │
│ │   [0,2] [1,2] [2,2] [3,2] [4,2] │
│ │                             │ │
│ │   線路 1: ● ─ ● ─ ● ─ ● ─ ● │ │
│ │                             │ │
│ └─────────────────────────────┘ │
│                                 │
│ 線路列表:                       │
│ ├─ 線 1: [0,1][1,1][2,1][3,1][4,1] │
│ ├─ 線 2: [0,0][1,0][2,0][3,0][4,0] │
│ ├─ ...                          │
│                                 │
│ [+ 新增線路] [重置為預設]        │
└─────────────────────────────────┘
```

#### 2.2.3 Tab: 視覺設定

```
┌─────────────────────────────────┐
│ 動畫參數                        │
│ ├─ 滾輪轉速: [====●===] 20     │
│ ├─ 旋轉時長: [====●===] 2000ms │
│ ├─ 停輪間隔: [====●===] 200ms  │
│ ├─ 緩停力度: [====●===] 0.5    │
│ └─ 回彈力度: [====●===] 0.3    │
├─────────────────────────────────┤
│ 盤面視覺                        │
│ ├─ 卷軸間距: [====●===] 10px   │
│ ├─ 圖示縮放: [====●===] 1.0x   │
│ └─ 盤面縮放: [====●===] 1.0x   │
├─────────────────────────────────┤
│ 素材上傳                        │
│ ├─ Symbol 圖片 (自動連動符號列表) │
│ │   [H1: 上傳] [H2: 上傳] ...   │
│ ├─ 盤面底圖: [上傳]             │
│ ├─ 盤面框: [上傳]               │
│ ├─ 背景: [上傳]                 │
│ └─ 人物: [上傳]                 │
└─────────────────────────────────┘
```

#### 2.2.4 Tab: Pool

```
┌─────────────────────────────────┐
│ 盤面模式                        │
│ ┌─────────────────────────────┐ │
│ │ [● 5x3]  [○ 5x4]            │ │
│ └─────────────────────────────┘ │
│ ⚠️ 切換模式將清空現有 Pool       │
├─────────────────────────────────┤
│ Build Pools                     │
│                                 │
│ [🔨 Build Pools]                │
│                                 │
│ 狀態: ✅ 已建立                  │
│ 上次建立: 2026-01-07 12:00:00   │
├─────────────────────────────────┤
│ Pool 狀態                       │
│ ┌─────────────────────────────┐ │
│ │ 未中獎 (NG): 100/100 盤面   │ │
│ │ 小獎 (NG): 100/100 盤面     │ │
│ │ 中獎 (NG): 100/100 盤面     │ │
│ │ 大獎 (NG): 50/100 盤面      │ │  ← 數量不足會標示警告
│ │ ...                         │ │
│ │ ─────────────────────────   │ │
│ │ 未中獎 (FG): 100/100 盤面   │ │
│ │ 小獎 (FG): 100/100 盤面     │ │
│ │ ...                         │ │
│ └─────────────────────────────┘ │
└─────────────────────────────────┘
```

### 2.3 右側 Game Control (25%)

#### 2.3.1 頂部快捷鍵

```
┌─────────────────────────────────────────┐
│ [🎰 SPIN] [📊 SIM] [🔄 AUTO SPIN]       │
└─────────────────────────────────────────┘
```

| 按鈕 | 功能 |
|------|------|
| SPIN | 執行單次 Spin |
| SIM | 開始 Simulation（根據當前模擬設定） |
| AUTO SPIN | 啟動/停止自動旋轉 |

#### 2.3.1.1 Auto Spin 功能規格

**行為描述**：
- 單擊 AUTO SPIN 按鈕啟動自動旋轉功能
- 啟動後，系統持續執行 Spin，直到用戶再次單擊按鈕取消
- 按鈕狀態在「啟動」和「停止」之間切換

**UI 狀態**：
| 狀態 | 按鈕顯示 | 按鈕樣式 |
|------|----------|----------|
| 未啟動 | 🔄 AUTO SPIN | 預設樣式 |
| 已啟動 | ⏹️ STOP | 高亮/警示樣式 |

**執行邏輯**：
```
1. 用戶點擊 AUTO SPIN
2. 設定 isAutoSpinning = true
3. 開始循環：
   a. 執行 Spin
   b. 等待動畫完成
   c. 檢查 isAutoSpinning
   d. 如果 true → 回到 3a
   e. 如果 false → 結束循環
4. 用戶再次點擊（STOP）
5. 設定 isAutoSpinning = false
6. 當前 Spin 完成後停止
```

**特殊情況處理**：
- 如果觸發 Free Spin，Auto Spin 會繼續執行完整的 Free Spin 流程
- Balance 不足時自動停止
- 切換盤面模式時自動停止

#### 2.3.2 Tab 切換器

```typescript
type GameControlTab = 'betting' | 'simulation' | 'history' | 'freespin';
```

#### 2.3.3 Tab: Betting

```
┌─────────────────────────────────┐
│ Betting                         │
│                                 │
│ Bet Amount                      │
│ [1] [5] [10] [25] [50] [100]   │
│ 當前: $10                       │
├─────────────────────────────────┤
│ Balance                         │
│ $10,000.00                      │
├─────────────────────────────────┤
│ Recent Win                      │
│ ┌─────────────────────────────┐ │
│ │ Spin #125  +$350.00         │ │
│ │ Spin #124  -$10.00          │ │
│ │ Spin #123  +$50.00          │ │
│ │ ...                         │ │
│ └─────────────────────────────┘ │
└─────────────────────────────────┘
```

#### 2.3.4 Tab: Simulation

```
┌─────────────────────────────────┐
│ Simulation                      │
│                                 │
│ 模擬次數                        │
│ [100] [500] [1000] [5000] [自訂] │
│ 當前: 1000 次                   │
├─────────────────────────────────┤
│ 模擬模式                        │
│ [● 堆疊模式] [○ 比較模式]        │
│                                 │
│ 💡 堆疊模式: 新結果累加至現有數據 │
│ 💡 比較模式: 保留前次結果並排比較 │
├─────────────────────────────────┤
│ 進度                            │
│ [████████░░░░░░░░] 52%          │
│ 520 / 1000 次                   │
├─────────────────────────────────┤
│ 累計統計                        │
│ 總模擬次數: 2,520 次            │
│ [清除累計]                      │
└─────────────────────────────────┘
```

#### 2.3.5 Tab: History

```
┌─────────────────────────────────┐
│ History                         │
│                                 │
│ 關鍵指標（即時更新）             │
│ ┌─────────────────────────────┐ │
│ │ RTP          96.52%         │ │
│ │ Hit Rate     28.3%          │ │
│ │ Avg Win      3.41x          │ │
│ │ Max Win      156.00x        │ │
│ │ Total Spins  2,520          │ │
│ │ Total Bet    $25,200        │ │
│ │ Total Win    $24,323        │ │
│ │ Net          -$877          │ │
│ └─────────────────────────────┘ │
├─────────────────────────────────┤
│ RTP 分解                        │
│ ┌─────────────────────────────┐ │
│ │ NG RTP       62.3%          │ │
│ │ FG RTP       34.2%          │ │
│ │ FG 觸發機率   8.5%           │ │
│ │ ─────────────────────       │ │
│ │ 總 RTP       96.5%          │ │
│ └─────────────────────────────┘ │
├─────────────────────────────────┤
│ 每局紀錄                        │
│ ┌─────────────────────────────┐ │
│ │ #2520 NG  $10 → +$35        │ │
│ │ #2519 FG  $0  → +$120       │ │
│ │ #2518 FG  $0  → +$45        │ │
│ │ #2517 NG  $10 → $0 (觸發FG) │ │
│ │ ...                         │ │
│ └─────────────────────────────┘ │
└─────────────────────────────────┘
```

#### 2.3.6 Tab: Free Spin

```
┌─────────────────────────────────┐
│ Free Spin                       │
│                                 │
│ 當前模式                        │
│ ┌─────────────────────────────┐ │
│ │ 🎰 [BASE GAME]              │ │  ← 或 [FREE GAME]
│ └─────────────────────────────┘ │
├─────────────────────────────────┤
│ Free Spin 狀態                  │
│ (僅在 Free Game 模式顯示)        │
│                                 │
│ 剩餘次數: 7 / 10                │
│ [████████░░░░░░░░░░░░]          │
│                                 │
│ 累積獎金: $1,250.00             │
│                                 │
│ Multiplier: 2x                  │
├─────────────────────────────────┤
│ 本輪 Free Spin 歷史             │
│ ┌─────────────────────────────┐ │
│ │ #3  +$450 (2x = $900)       │ │
│ │ #2  +$120 (2x = $240)       │ │
│ │ #1  +$55  (2x = $110)       │ │
│ └─────────────────────────────┘ │
└─────────────────────────────────┘
```

### 2.4 底部統計區

```
┌─────────────────────────────────────────────────────────────┐
│ STATISTICS & CHARTS                                   [▲]   │
├─────────────────────────────────────────────────────────────┤
│ ┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐ │
│ │ Winnings        │ │ Balance History │ │ Symbol Dist.    │ │
│ │                 │ │                 │ │                 │ │
│ │  📊 柱狀圖       │ │  📈 折線圖       │ │  🥧 圓餅圖       │ │
│ │                 │ │                 │ │                 │ │
│ │                 │ │                 │ │                 │ │
│ └─────────────────┘ └─────────────────┘ └─────────────────┘ │
│                                                             │
│ [匯出 CSV]                                                  │
└─────────────────────────────────────────────────────────────┘
```

### 2.5 彈窗設計

#### 2.5.1 盤面模式切換確認

```
┌─────────────────────────────────┐
│ ⚠️ 確認切換盤面模式              │
├─────────────────────────────────┤
│                                 │
│ 你即將從 5x3 切換到 5x4 模式。   │
│                                 │
│ 此操作將會：                     │
│ • 清空所有已建立的 Pool          │
│ • 重置 Pay Lines 配置            │
│                                 │
│ 確定要繼續嗎？                   │
│                                 │
│        [取消]  [確認切換]        │
└─────────────────────────────────┘
```

---

## 三、模組 B：遊戲機制模組

### 3.1 Symbol 系統擴展

#### 3.1.1 Symbol 類別

```typescript
type SymbolType = 'normal' | 'wild' | 'scatter';
```

| 類別 | 說明 | 特殊行為 |
|------|------|----------|
| `normal` | 一般符號 | 無 |
| `wild` | 百搭符號 | 可替代其他符號 |
| `scatter` | 分散符號 | 觸發 Free Spin |

#### 3.1.2 Wild 符號邏輯

```typescript
interface WildConfig {
  canReplaceNormal: boolean;   // 是否可替代一般符號（預設 true）
  canReplaceSpecial: boolean;  // 是否可替代特殊符號（預設 false）
}
```

**結算邏輯修改**：
```
原邏輯：檢查連續相同符號
新邏輯：
1. 檢查連續相同符號
2. 如果位置是 Wild，且該 Wild 可替代目標符號，視為相同
3. Wild 不計算 Wild 自身的 payout（僅作為替代）
4. 選擇最佳組合（Wild 可組成多種組合時，選最高分）
```

#### 3.1.3 Scatter 符號邏輯

```typescript
interface ScatterConfig {
  triggerCount: number;        // 觸發所需數量（預設 3）
  freeSpinCount: number;       // 給予的 Free Spin 次數
  enableRetrigger: boolean;    // 是否支援 Retrigger
  enableMultiplier: boolean;   // 是否啟用 Multiplier
  multiplierValue: number;     // Multiplier 倍率（預設 2）
}
```

**觸發邏輯**：
```
1. Scatter 不需要在 Pay Line 上
2. 只要盤面上出現 >= triggerCount 個 Scatter，即觸發
3. 觸發時，進入 Free Game 模式
4. Free Game 期間使用 FG Outcomes
```

### 3.2 Free Spin 機制

#### 3.2.1 Free Spin 狀態機

```
┌─────────────┐     觸發條件達成      ┌─────────────┐
│  BASE GAME  │ ─────────────────→ │  FREE GAME  │
│    (NG)     │                     │    (FG)     │
└─────────────┘ ←───────────────── └─────────────┘
                  Free Spin 用完
```

#### 3.2.2 Free Spin 流程

```
1. [NG] 玩家按 SPIN
2. [NG] 抽取 NG Outcome → 從 NG Pool 取盤面
3. [NG] 結算（檢查是否有 Scatter 觸發）
4. [NG] 如果 Scatter >= triggerCount：
   a. 記錄觸發前的獎金
   b. 切換到 FG 模式
   c. 設定 Free Spin 剩餘次數
   d. 設定 Multiplier（如啟用）
5. [FG] 自動執行 Free Spin
6. [FG] 抽取 FG Outcome → 從 FG Pool 取盤面
7. [FG] 結算（獎金 × Multiplier）
8. [FG] 檢查 Retrigger（如啟用且 Scatter >= triggerCount）
9. [FG] 剩餘次數 > 0 → 回到步驟 5
10. [FG] 剩餘次數 = 0 → 結算總獎金，返回 NG 模式
```

#### 3.2.3 RTP 計算公式

根據業界標準：

```
總 RTP = NG RTP + FG RTP 貢獻

FG RTP 貢獻 = P(觸發FG) × E[FG 總獎金] / Bet

其中：
- P(觸發FG) = Scatter >= N 的機率
- E[FG 總獎金] = 預期 Free Spin 次數 × 平均每次 FG 獎金 × Multiplier

預期 Free Spin 次數（含 Retrigger）：
= 初始次數 / (1 - 初始次數 × P(Retrigger))
```

### 3.3 盤面系統擴展

#### 3.3.1 動態盤面

```typescript
interface BoardConfig {
  cols: 5;              // 固定 5 列
  rows: 3 | 4;          // 支援 3 或 4 行
}
```

#### 3.3.2 切換邏輯

```
切換盤面模式時：
1. 彈出確認對話框
2. 用戶確認後：
   a. 清空所有 Pool（NG + FG）
   b. 重置 Pay Lines 為該模式的預設配置
   c. 更新 Board 配置
3. 用戶需重新 Build Pools
```

### 3.4 Pay Lines 擴展

#### 3.4.1 視覺化編輯器

```
功能：
1. 顯示盤面格子（5xN）
2. 用戶點擊格子設定線路
3. 每條線必須經過每一列（5 個點）
4. 拖拉調整已有線路
5. 新增/刪除線路
6. 最多 50 條線
```

#### 3.4.2 預設線路配置

| 盤面 | 預設線數 | 說明 |
|------|----------|------|
| 5x3 | 20 條 | 繼承 MVP 配置 |
| 5x4 | 30 條 | 新增 10 條利用第 4 行 |

---

## 四、模組 C：數據與統計模組

### 4.1 RTP 分析

#### 4.1.1 顯示指標

| 指標 | 說明 | 計算方式 |
|------|------|----------|
| NG RTP | Base Game 貢獻 | NG 總贏分 / 總下注 |
| FG RTP | Free Game 貢獻 | FG 總贏分 / 總下注 |
| FG 觸發機率 | Free Game 觸發率 | 觸發次數 / NG Spin 次數 |
| 總 RTP | 合併後的 RTP | NG RTP + FG RTP |

#### 4.1.2 理論值計算

```typescript
interface TheoreticalRTP {
  ngRTP: number;              // 理論 NG RTP
  fgRTP: number;              // 理論 FG RTP
  fgTriggerProbability: number; // 理論 FG 觸發機率
  totalRTP: number;           // 理論總 RTP
}
```

### 4.2 Simulation 堆疊/比較

#### 4.2.1 堆疊模式

```
操作：
1. 執行 Simulation 100 次 → 結果 A
2. 執行 Simulation 100 次 → 結果 B
3. 累計顯示 200 次的統計

數據結構：
{
  totalSpins: 200,
  totalBet: 2000,
  totalWin: 1930,
  results: [...A.results, ...B.results]
}
```

#### 4.2.2 比較模式

```
操作：
1. 執行 Simulation 100 次 → 結果 A
2. 執行 Simulation 100 次 → 結果 B
3. 並排顯示 A 和 B 的統計

顯示：
| 指標       | 批次 A  | 批次 B  | 差異    |
|-----------|---------|---------|---------|
| RTP       | 95.2%   | 97.8%   | +2.6%   |
| Hit Rate  | 27.0%   | 29.5%   | +2.5%   |
| ...       | ...     | ...     | ...     |
```

### 4.3 數值驗證工具

#### 4.3.1 賠率分佈圖

```
顯示：
- X 軸：倍率區間
- Y 軸：出現次數/機率
- 對照：實際分佈 vs 理論分佈

用途：
- 驗證 Pool 生成是否符合 Outcome 設定
- 檢測異常分佈
```

#### 4.3.2 矛盾警示

```
檢測情境：
1. Outcome 設定的倍率區間無法達成（符號 payout 不足）
2. Pool 生成數量不足
3. NG/FG 設定衝突
4. Wild 設定可能導致的極端情況

警示等級：
- 🔴 錯誤（Error）：必須修正才能 Build
- 🟡 警告（Warning）：可能影響 RTP 準確性
- 🔵 提示（Info）：建議優化項目
```

---

## 五、模組 D：用戶功能模組

### 5.1 Firebase 用戶系統

#### 5.1.1 登入方式

- Google OAuth（主要）
- 未來可擴展：Email/Password、其他 OAuth

#### 5.1.2 用戶狀態

```typescript
type UserState = 
  | { type: 'guest' }                    // 訪客
  | { type: 'authenticated'; user: User } // 已登入
```

#### 5.1.3 訪客模式限制

| 功能 | 訪客 | 登入用戶 |
|------|------|----------|
| 使用 Default 模板 | ✅ | ✅ |
| 調整參數 | ✅ | ✅ |
| Spin / Simulation | ✅ | ✅ |
| 儲存模板 | ❌ | ✅ |
| 匯出規格書 | ❌ | ✅ |
| 雲端同步 | ❌ | ✅ |

### 5.2 模板管理

#### 5.2.1 模板結構

```typescript
interface Template {
  id: string;
  name: string;
  userId: string;
  createdAt: Timestamp;
  updatedAt: Timestamp;
  config: {
    gameName: string;
    baseBet: number;
    boardConfig: BoardConfig;
    symbols: SymbolDefinition[];
    ngOutcomes: Outcome[];
    fgOutcomes: Outcome[];
    linesConfig: LinesConfig;
    visualConfig: VisualConfig;
    freeSpinConfig: FreeSpinConfig;
  };
  // 素材另存於 Firebase Storage
  assetUrls?: {
    symbols?: Record<string, string>;
    board?: string;
    frame?: string;
    background?: string;
    character?: string;
  };
}
```

#### 5.2.2 模板操作

| 操作 | 說明 |
|------|------|
| 新建 | 從 Default 或空白建立 |
| 儲存 | 儲存當前設定到雲端 |
| 讀取 | 從雲端載入模板 |
| 刪除 | 刪除雲端模板 |
| 重新命名 | 修改模板名稱 |

### 5.3 Dashboard

#### 5.3.1 Dashboard 佈局

```
┌─────────────────────────────────────────────────────────────┐
│  🎰 slot-ide                              [👤 用戶名] [登出] │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  👋 歡迎回來，用戶名                                         │
│                                                             │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │ 快速操作                                                 │ │
│  │ [+ 新建模板]  [📂 匯入]  [📖 教學]                       │ │
│  └─────────────────────────────────────────────────────────┘ │
│                                                             │
│  📁 我的模板                                                │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐           │
│  │ 🎰          │ │ 🎰          │ │ 🎰          │           │
│  │ 模板 A      │ │ 模板 B      │ │ 模板 C      │           │
│  │ 更新: 1/7   │ │ 更新: 1/5   │ │ 更新: 1/3   │           │
│  │ [開啟][刪除]│ │ [開啟][刪除]│ │ [開啟][刪除]│           │
│  └─────────────┘ └─────────────┘ └─────────────┘           │
│                                                             │
│  🕐 最近編輯                                                │
│  ├─ 模板 A - 2026-01-07 12:00                              │
│  ├─ 模板 B - 2026-01-05 15:30                              │
│  └─ 模板 C - 2026-01-03 09:15                              │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 六、模組 E：基礎設施模組

### 6.1 狀態管理擴展

#### 6.1.1 新增 Store

```typescript
// useAuthStore.ts - 用戶認證狀態
interface AuthState {
  user: User | null;
  isLoading: boolean;
  error: string | null;
}

// useFreeSpinStore.ts - Free Spin 狀態
interface FreeSpinState {
  mode: 'base' | 'free';
  remainingSpins: number;
  totalSpins: number;
  accumulatedWin: number;
  multiplier: number;
  history: FreeSpinResult[];
}

// useSimulationStore.ts - Simulation 狀態
interface SimulationState {
  isRunning: boolean;
  progress: number;
  mode: 'stack' | 'compare';
  results: SimulationResult[];
  compareResults: SimulationResult[][];  // 比較模式用
}

// useTemplateStore.ts - 模板管理狀態
interface TemplateState {
  templates: Template[];
  currentTemplateId: string | null;
  isLoading: boolean;
  isSaving: boolean;
}
```

### 6.2 Firebase 整合

#### 6.2.1 Firebase 服務

```typescript
// firebase/config.ts
const firebaseConfig = {
  apiKey: "...",
  authDomain: "...",
  projectId: "...",
  storageBucket: "...",
  messagingSenderId: "...",
  appId: "..."
};

// firebase/auth.ts
export const signInWithGoogle = async (): Promise<User>
export const signOut = async (): Promise<void>
export const onAuthStateChanged = (callback: (user: User | null) => void)

// firebase/firestore.ts
export const saveTemplate = async (template: Template): Promise<string>
export const getTemplates = async (userId: string): Promise<Template[]>
export const deleteTemplate = async (templateId: string): Promise<void>

// firebase/storage.ts
export const uploadAsset = async (file: File, path: string): Promise<string>
export const deleteAsset = async (path: string): Promise<void>
```

### 6.3 專案結構擴展

```
src/
├── main.tsx
├── App.tsx
├── types/
│   ├── index.ts
│   ├── spin-packet.ts
│   ├── outcome.ts
│   ├── symbol.ts          # 擴展 Wild/Scatter
│   ├── board.ts           # 擴展 5x4
│   ├── lines.ts
│   ├── visual.ts
│   ├── free-spin.ts       # 新增
│   ├── template.ts        # 新增
│   └── user.ts            # 新增
├── engine/
│   ├── index.ts
│   ├── outcome-manager.ts
│   ├── symbol-manager.ts
│   ├── lines-manager.ts
│   ├── pool-builder.ts    # 擴展 NG/FG Pool
│   ├── spin-executor.ts   # 擴展 Free Spin 邏輯
│   ├── settlement.ts      # 擴展 Wild 結算
│   └── rtp-calculator.ts  # 新增
├── runtime/
│   ├── index.ts
│   ├── SlotMachine.tsx
│   ├── Reel.tsx
│   └── Symbol.tsx
├── ide/
│   ├── index.ts
│   ├── layout/
│   │   ├── IDELayout.tsx      # 重構為三欄式
│   │   ├── ControlPanel.tsx   # 左側
│   │   ├── GameControl.tsx    # 右側
│   │   └── StatisticsPanel.tsx # 底部
│   └── panels/
│       ├── OutcomePanel.tsx   # 擴展 NG/FG 切換
│       ├── SymbolPanel.tsx    # 擴展 Wild/Scatter
│       ├── LinesPanel.tsx     # 擴展視覺化編輯
│       ├── AnimationPanel.tsx
│       ├── LayoutPanel.tsx
│       ├── AssetPanel.tsx
│       ├── PoolPanel.tsx      # 新增
│       ├── BettingPanel.tsx   # 新增
│       ├── SimulationPanel.tsx # 新增
│       ├── HistoryPanel.tsx   # 新增
│       └── FreeSpinPanel.tsx  # 新增
├── analytics/
│   ├── index.ts
│   ├── simulator.ts       # 擴展堆疊/比較
│   ├── charts.tsx
│   ├── csv-export.ts
│   └── validation.ts      # 新增（數值驗證）
├── store/
│   ├── index.ts
│   ├── useGameConfigStore.ts  # 擴展
│   ├── useUIStore.ts
│   ├── useAuthStore.ts        # 新增
│   ├── useFreeSpinStore.ts    # 新增
│   ├── useSimulationStore.ts  # 新增
│   └── useTemplateStore.ts    # 新增
├── firebase/                  # 新增
│   ├── config.ts
│   ├── auth.ts
│   ├── firestore.ts
│   └── storage.ts
├── pages/                     # 新增
│   ├── Dashboard.tsx
│   └── Editor.tsx
└── utils/
    ├── index.ts
    └── asset-storage.ts
```

---

## 七、型別定義擴展

### 7.1 Symbol 型別擴展

```typescript
// src/types/symbol.ts

import type { SymbolId } from './board.js';

/**
 * Symbol 類別
 */
export type SymbolCategory = 'high' | 'low';

/**
 * Symbol 類型
 */
export type SymbolType = 'normal' | 'wild' | 'scatter';

/**
 * Wild 設定
 */
export interface WildConfig {
  canReplaceNormal: boolean;   // 是否可替代一般符號（預設 true）
  canReplaceSpecial: boolean;  // 是否可替代特殊符號（預設 false）
}

/**
 * Scatter 設定
 */
export interface ScatterConfig {
  triggerCount: number;        // 觸發所需數量（預設 3）
  freeSpinCount: number;       // 給予的 Free Spin 次數
  enableRetrigger: boolean;    // 是否支援 Retrigger
  enableMultiplier: boolean;   // 是否啟用 Multiplier
  multiplierValue: number;     // Multiplier 倍率（預設 2）
}

/**
 * Symbol 定義（擴展版）
 */
export interface SymbolDefinition {
  id: SymbolId;
  name: string;
  type: SymbolType;            // 新增：符號類型
  category: SymbolCategory;    // 僅 normal 類型有效
  payouts: {
    match3: number;
    match4: number;
    match5: number;
  };
  // 雙層機率模型
  appearanceWeight: number;    // 視覺層：滾動動畫用
  ngWeight: number;            // 數學層：NG Pool 生成用
  fgWeight: number;            // 數學層：FG Pool 生成用
  // 特殊符號設定
  wildConfig?: WildConfig;     // 僅 wild 類型有效
  scatterConfig?: ScatterConfig; // 僅 scatter 類型有效
}
```

### 7.2 Board 型別擴展

```typescript
// src/types/board.ts

export type SymbolId = string;

export type BoardRows = 3 | 4;

export interface BoardConfig {
  cols: 5;
  rows: BoardRows;
}

export interface Board {
  reels: SymbolId[][];  // 5 列，每列 3 或 4 個符號
  cols: 5;
  rows: BoardRows;
}
```

### 7.3 Outcome 型別擴展

```typescript
// src/types/outcome.ts

export type GamePhase = 'ng' | 'fg';

export interface Outcome {
  id: string;
  name: string;
  phase: GamePhase;         // 新增：所屬遊戲階段
  multiplierRange: {
    min: number;
    max: number;
  };
  weight: number;
}

export interface OutcomeConfig {
  ngOutcomes: Outcome[];    // Base Game Outcomes
  fgOutcomes: Outcome[];    // Free Game Outcomes
}
```

### 7.4 Free Spin 型別

```typescript
// src/types/free-spin.ts

export type FreeSpinMode = 'base' | 'free';

export interface FreeSpinConfig {
  enabled: boolean;            // 是否啟用 Free Spin 功能
  triggerCount: number;        // Scatter 觸發數量
  baseSpinCount: number;       // 基礎 Free Spin 次數
  enableRetrigger: boolean;    // 是否支援 Retrigger
  retriggerSpinCount: number;  // Retrigger 給予的額外次數
  enableMultiplier: boolean;   // 是否啟用 Multiplier
  multiplierValue: number;     // Multiplier 倍率
}

export interface FreeSpinState {
  mode: FreeSpinMode;
  remainingSpins: number;
  totalSpins: number;
  accumulatedWin: number;
  currentMultiplier: number;
  triggerCount: number;        // 本輪觸發的 Scatter 數量
}

export interface FreeSpinResult {
  spinIndex: number;
  board: Board;
  win: number;
  multipliedWin: number;
  isRetrigger: boolean;
}
```

### 7.5 SpinPacket 擴展

```typescript
// src/types/spin-packet.ts

import type { Board } from './board.js';
import type { SymbolId } from './board.js';
import type { VisualConfig } from './visual.js';
import type { AssetsPatch } from './visual.js';
import type { FreeSpinMode, FreeSpinState } from './free-spin.js';

export interface WinningLine {
  lineIndex: number;
  positions: [number, number][];
  symbol: SymbolId;
  count: number;
  payout: number;
  hasWild: boolean;           // 新增：是否包含 Wild
  wildPositions?: [number, number][]; // 新增：Wild 位置
}

export interface SettlementMeta {
  outcomeId: string;
  phase: FreeSpinMode;        // 新增：遊戲階段
  win: number;
  multiplier: number;
  winningLines: WinningLine[];
  bestLine?: WinningLine;
  scatterCount: number;       // 新增：Scatter 數量
  triggeredFreeSpin: boolean; // 新增：是否觸發 Free Spin
}

export interface SpinPacket {
  version: "2";               // 版本升級
  board: Board;
  visual: VisualConfig;
  assets?: AssetsPatch;
  meta?: SettlementMeta;
  freeSpinState?: FreeSpinState; // 新增：Free Spin 狀態
}
```

### 7.6 Template 型別

```typescript
// src/types/template.ts

import type { BoardConfig } from './board.js';
import type { SymbolDefinition } from './symbol.js';
import type { OutcomeConfig } from './outcome.js';
import type { LinesConfig } from './lines.js';
import type { VisualConfig, AssetsPatch } from './visual.js';
import type { FreeSpinConfig } from './free-spin.js';

export interface Template {
  id: string;
  name: string;
  userId: string;
  createdAt: Date;
  updatedAt: Date;
  config: TemplateConfig;
  assetUrls?: AssetUrls;
}

export interface TemplateConfig {
  gameName: string;
  baseBet: number;
  boardConfig: BoardConfig;
  symbols: SymbolDefinition[];
  outcomeConfig: OutcomeConfig;
  linesConfig: LinesConfig;
  visualConfig: VisualConfig;
  freeSpinConfig: FreeSpinConfig;
}

export interface AssetUrls {
  symbols?: Record<string, string>;
  board?: string;
  frame?: string;
  background?: string;
  character?: string;
}
```

### 7.7 User 型別

```typescript
// src/types/user.ts

export interface User {
  uid: string;
  email: string;
  displayName: string;
  photoURL?: string;
  createdAt: Date;
  lastLoginAt: Date;
}

export type AuthStatus = 'loading' | 'authenticated' | 'guest';
```

---

## 八、開發階段規劃

### Phase 1：核心機制擴展（優先）

| 任務 | 預估時間 | 依賴 |
|------|----------|------|
| 1.1 型別定義擴展 | 1 天 | 無 |
| 1.2 Symbol 系統擴展（Wild/Scatter） | 2 天 | 1.1 |
| 1.3 Settlement 邏輯修改（Wild 結算） | 2 天 | 1.2 |
| 1.4 Free Spin 機制實作 | 3 天 | 1.3 |
| 1.5 Board 5x4 支援 | 1 天 | 1.1 |
| 1.6 Pool Builder 擴展（NG/FG） | 2 天 | 1.4 |
| 1.7 RTP Calculator（NG/FG 分開） | 1 天 | 1.6 |

**預估總時間：12 天**

### Phase 2：UI 重構

| 任務 | 預估時間 | 依賴 |
|------|----------|------|
| 2.1 三欄式佈局骨架 | 2 天 | Phase 1 |
| 2.2 左側 Control Panel | 3 天 | 2.1 |
| 2.3 右側 Game Control | 2 天 | 2.1 |
| 2.4 底部統計區 | 1 天 | 2.1 |
| 2.5 SymbolPanel 擴展（Wild/Scatter UI） | 2 天 | 2.2 |
| 2.6 OutcomePanel 擴展（NG/FG 切換） | 1 天 | 2.2 |
| 2.7 Pay Lines 視覺化編輯器 | 3 天 | 2.2 |
| 2.8 Free Spin Panel | 1 天 | 2.3 |

**預估總時間：15 天**

### Phase 3：用戶系統與雲端

| 任務 | 預估時間 | 依賴 |
|------|----------|------|
| 3.1 Firebase 專案設定 | 0.5 天 | 無 |
| 3.2 Auth 模組（Google OAuth） | 1 天 | 3.1 |
| 3.3 Firestore 模板 CRUD | 2 天 | 3.1 |
| 3.4 Storage 素材上傳 | 1 天 | 3.1 |
| 3.5 Dashboard 頁面 | 2 天 | 3.2, 3.3 |
| 3.6 訪客模式處理 | 1 天 | 3.2 |

**預估總時間：7.5 天**

### Phase 4：進階功能

| 任務 | 預估時間 | 依賴 |
|------|----------|------|
| 4.1 Simulation 堆疊/比較模式 | 2 天 | Phase 2 |
| 4.2 數值驗證工具 | 2 天 | Phase 1 |
| 4.3 深色模式 | 1 天 | Phase 2 |
| 4.4 最終整合測試 | 2 天 | All |

**預估總時間：7 天**

### 總時間估算

| Phase | 時間 |
|-------|------|
| Phase 1 | 12 天 |
| Phase 2 | 15 天 |
| Phase 3 | 7.5 天 |
| Phase 4 | 7 天 |
| **總計** | **~42 天** |

---

## 九、驗收清單

### Phase 1 驗收

- [ ] Wild 符號可正確替代一般符號
- [ ] Wild 符號不會替代特殊符號
- [ ] Scatter 觸發 Free Spin 正確
- [ ] Free Spin 次數正確扣減
- [ ] Retrigger 功能正常
- [ ] Multiplier 計算正確
- [ ] 5x4 盤面可正常生成
- [ ] NG/FG Pool 分開建立
- [ ] RTP 分開顯示（NG/FG/總）

### Phase 2 驗收

- [ ] 三欄式佈局正確顯示
- [ ] 左側 Tab 切換正常
- [ ] 右側 Tab 切換正常
- [ ] 底部統計區可收合
- [ ] SymbolPanel Wild/Scatter 設定正常
- [ ] OutcomePanel NG/FG 切換正常
- [ ] Pay Lines 視覺化編輯可用
- [ ] 盤面模式切換有確認彈窗

### Phase 3 驗收

- [ ] Google 登入/登出正常
- [ ] 模板可儲存到雲端
- [ ] 模板可從雲端讀取
- [ ] 模板可刪除
- [ ] Dashboard 顯示正確
- [ ] 訪客無法儲存/匯出

### Phase 4 驗收

- [ ] Simulation 堆疊模式正確累加
- [ ] Simulation 比較模式並排顯示
- [ ] 數值驗證工具可檢測矛盾
- [ ] 深色模式可切換

---

## 附錄：P0 Gate 擴展

新增檢查項目：

- [ ] **Wild 結算唯一** — 只有一處 Wild 替代邏輯
- [ ] **Free Spin 狀態唯一** — 只有一個 Free Spin 狀態機
- [ ] **NG/FG Pool 分離** — 兩種 Pool 不混用
- [ ] **Firebase 安全規則** — 用戶只能存取自己的數據
- [ ] **SpinPacket v2 相容** — 舊版 v1 可升級或降級處理

---

**文件結束**

